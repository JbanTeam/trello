export const notEmptyRules = [val => !!val || 'Cannot be empty'];

/* export const confirmPasswordRules = [
  confirmPassword =>
    confirmPassword === vm.user.password || 'Password must match'
]; */
